import { createTheme } from '@mui/material/styles';


//TODO create in separate files by design tokens (i.e.: typography, palettes, spacing, etc)
//TODO and import here and compose the theme
const theme = createTheme({

});

export default theme;